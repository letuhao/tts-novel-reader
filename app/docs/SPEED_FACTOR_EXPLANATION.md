# Why Audio Sounds Different - Speed Factor Explanation / Tại sao Audio Nghe Khác - Giải thích Hệ số Tốc độ

## 🔍 The Issue / Vấn đề

The preset file `dia_female_05.wav` sounds different from the generated chapter audio because of **speed resampling**.

File preset `dia_female_05.wav` nghe khác với audio chapter được tạo vì **lấy mẫu lại tốc độ** (speed resampling).

## 📊 Comparison / So sánh

### Preset File (`dia_female_05.wav`)
- **Speed Factor:** `1.0` (normal speed - no resampling)
- **Generated:** Without speed control
- **Quality:** Original model output (unchanged)
- **Pitch/Tone:** Natural, as generated by model

### Current Chapter Audio (with speed_factor = 0.85)
- **Speed Factor:** `0.85` (15% slower)
- **Method:** Audio resampling after generation
- **Quality:** May have slight artifacts from resampling
- **Pitch/Tone:** Can sound slightly different due to resampling

### New Chapter Audio (speed_factor = 1.0)
- **Speed Factor:** `1.0` (normal speed - same as preset)
- **Method:** No resampling (original model output)
- **Quality:** Should match preset quality
- **Pitch/Tone:** Should match preset voice

## 🎯 Solution / Giải pháp

### Option 1: Use Normal Speed (1.0x) - Matches Preset
```javascript
speedFactor: 1.0  // Same as preset - no resampling
```
**Pros:**
- Matches preset voice quality
- Natural pitch/tone
- No resampling artifacts

**Cons:**
- Faster speech (harder to follow)

### Option 2: Use Slower Speed (0.85x) - Better Comprehension
```javascript
speedFactor: 0.85  // 15% slower for comprehension
```
**Pros:**
- Slower speech (easier to understand)
- More time to process story

**Cons:**
- Slight pitch/tone change from resampling
- May sound different from preset

### Option 3: Slower but Acceptable (0.90x)
```javascript
speedFactor: 0.90  // 10% slower - minimal resampling effect
```
**Pros:**
- Slower than normal
- Less resampling artifact (minimal change)

**Cons:**
- Still slightly different from preset

## 📈 Test Results / Kết quả Kiểm tra

### Version 1.0x (Normal Speed)
- **File ID:** `65eb207cf31d592b38e2a734cf5f721d`
- **URL:** `http://127.0.0.1:8000/api/tts/audio/65eb207cf31d592b38e2a734cf5f721d`
- **Quality:** Should match preset `dia_female_05.wav`
- **Speed:** Normal (fast, but natural voice)

### Version 0.85x (15% Slower)
- **File ID:** `bb6acc76d3ea1a049ca8552fda96fe0b`
- **URL:** `http://127.0.0.1:8000/api/tts/audio/bb6acc76d3ea1a049ca8552fda96fe0b`
- **Quality:** Resampled (may sound slightly different)
- **Speed:** 15% slower (easier to follow)

## 💡 Recommendation / Khuyến nghị

**For matching preset quality:**
- Use `speedFactor: 1.0` (normal speed)
- Trade-off: Faster speech (harder to follow)

**For better comprehension:**
- Use `speedFactor: 0.85` (15% slower)
- Trade-off: Slight voice difference (resampling effect)

**Best compromise:**
- Use `speedFactor: 0.90` (10% slower)
- Minimal resampling effect, slightly slower than normal

## 🔧 How to Fix / Cách Sửa

To match the preset exactly:
1. Set `speedFactor: 1.0` (normal speed)
2. Use playback speed control in audio player instead
3. Or accept slight voice difference for slower comprehension

---

**Speed resampling changes voice quality - that's why it sounds different!**  
**Lấy mẫu lại tốc độ thay đổi chất lượng giọng - đó là lý do nghe khác!**

