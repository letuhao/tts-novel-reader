# TTS Backend Requirements - Using VieNeu-TTS compatible versions
# Yêu cầu TTS Backend - Sử dụng phiên bản tương thích VieNeu-TTS

# Base dependencies from VieNeu-TTS (working setup)
# Phụ thuộc cơ bản từ VieNeu-TTS (setup hoạt động)
phonemizer>=3.3.0
torch
torchvision
torchaudio
neucodec>=0.0.4
librosa>=0.11.0
huggingface-hub[cli]>=0.36.0
soundfile>=0.13.1

# API & Web Framework / API và Framework Web
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.11.3
python-multipart>=0.0.9

# Dia TTS dependencies (optional - only if using Dia model)
# Phụ thuộc Dia TTS (tùy chọn - chỉ nếu sử dụng model Dia)
descript-audio-codec>=1.0.0
transformers>=4.35.0
bitsandbytes>=0.39.0

# Utilities / Tiện ích
numpy>=2.2.4
safetensors>=0.4.0
tqdm>=4.65.0

# Optional: For Gradio interface / Tùy chọn: Cho giao diện Gradio
# gradio>=5.49.1

